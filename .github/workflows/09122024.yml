name: Run on Path Change

on:
  push:
    paths:
      - 'src/**'
      - '!src/pradeep/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Run a script
        run: echo "Am inside 09122024 file path!"
