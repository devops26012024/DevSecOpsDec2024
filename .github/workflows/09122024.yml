name: Run on Path Change

on:
  push:
    paths:
      - 'src/**'
      - 'file.txt'
      - '!src/pradeep/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Run a script
        run: echo "Workflow triggered by changes in 'src/**' or 'file.txt'."
