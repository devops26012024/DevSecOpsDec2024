name: Run on Path Change

on:
  push:
    paths:
      - "/.github/workflows/pradeep/**"         # Match all files in the src/ directory

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Run a script
        run: echo "Am inside pradeep dir path!"
